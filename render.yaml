services:
  - type: web
    name: voice-memory-vault
    env: node
    buildCommand: npm run build
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: 20.0.0
    disk:
      name: vault-data
      mountPath: /var/data
      sizeGB: 1
